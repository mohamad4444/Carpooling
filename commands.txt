rideshare-app
	mvn clean install -DskipTests 
docker-compose build
docker-compose up

# Optional with volumes for development
Dockerfile
	FROM eclipse-temurin:21-alpine
	WORKDIR /app
	ENTRYPOINT ["java", "-jar", "app.jar"]
Docker-compsoe configuration
	volumes:
	  - ./rideshare-app/target/app.jar:/app/app.jar